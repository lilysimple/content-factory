"До / после" contrast scheme — neutral tile → terracotta arrow → graphite tile.

```jsx
<BeforeAfter
  beforeLabel="Без AI" beforeValue={<>Ручной отчёт<br/>4 часа</>}
  afterLabel="С AI"   afterValue={<>Авто-отчёт<br/>15 минут</>}
/>
```

Keep values short — one figure or a two-line phrase each.
